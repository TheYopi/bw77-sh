# Generated by BW77-Shell. Source from hyprland.conf with:
#   source = ~/.config/hypr/bw77.conf
general {
    col.active_border = rgb({{borderActiveFrom.hex}}) rgb({{borderActiveTo.hex}}) {{borderAngle}}deg
    col.inactive_border = rgb({{borderInactive.hex}})
    border_size = {{borderWidth}}
    rounding = {{borderRadius}}
}

decoration {
    rounding = {{borderRadius}}
}

group {
    col.border_active = rgb({{borderActiveFrom.hex}})
    col.border_inactive = rgb({{borderInactive.hex}})
}
